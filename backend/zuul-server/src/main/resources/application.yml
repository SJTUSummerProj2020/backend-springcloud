server:
  port: 8802
spring:
  application:
    name: zuul-server
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
management:
  endpoints:
    web:
      exposure:
        include: health,info,hysrix,stream,routes
zuul:
#  prefix: /api
  routes:
    user-service: /user/**
    goods-service: /goods/**
    order-service: /order/**
